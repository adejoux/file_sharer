= simple_form_for @document, :html => {:multipart => true} do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :file
    = f.hidden_field :folder_id, value: @folder

  .form-actions
    = f.button :submit
